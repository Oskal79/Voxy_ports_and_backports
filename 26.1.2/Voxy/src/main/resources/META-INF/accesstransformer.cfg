# Voxy access transformer (NeoForge)
# Ported 1:1 from the Fabric access widener (src/main/resources/voxy.accesswidener).
# AW "accessible" == AT "public"; none of the entries were "mutable", so no final-stripping (public-f) is needed.

# --- classes ---
public net.minecraft.client.multiplayer.ClientChunkCache$Storage
public com.mojang.blaze3d.opengl.GlDebug$LogEntry
public com.mojang.blaze3d.opengl.GlRenderPass
# PalettedContainer$Data is package-private in vanilla. On Fabric, Loom applied Sodium's OWN access
# widener to dependents, so Voxy got this for free; NeoForge does not inherit a dependency's access
# transformers at compile time, so Voxy has to declare it itself.
public net.minecraft.world.level.chunk.PalettedContainer$Data
# Widening a record type requires widening its canonical constructor too (NeoForge's AT validator
# enforces this). Same pair Sodium declares in its own accesstransformer.cfg.
public net.minecraft.world.level.chunk.PalettedContainer$Data <init>(Lnet/minecraft/world/level/chunk/Configuration;Lnet/minecraft/util/BitStorage;Lnet/minecraft/world/level/chunk/Palette;)V
# The access widener only listed GlCommandEncoder's methods, but Fabric's AW spec also makes the
# owning class accessible; AT has no such implicit rule, so the class needs its own entry.
public com.mojang.blaze3d.opengl.GlCommandEncoder

# --- methods ---
public com.mojang.blaze3d.opengl.GlCommandEncoder trySetup(Lcom/mojang/blaze3d/opengl/GlRenderPass;Ljava/util/Collection;)Z
public com.mojang.blaze3d.opengl.GlCommandEncoder applyPipelineState(Lcom/mojang/blaze3d/pipeline/RenderPipeline;)V
public net.minecraft.client.multiplayer.ClientChunkCache$Storage getChunk(I)Lnet/minecraft/world/level/chunk/LevelChunk;
public net.minecraft.client.multiplayer.ClientChunkCache$Storage getIndex(II)I

# --- fields ---
public com.mojang.blaze3d.opengl.GlBuffer handle
public net.minecraft.client.renderer.texture.TextureAtlas maxMipLevel
public net.minecraft.client.gui.components.BossHealthOverlay events
public net.minecraft.client.multiplayer.MultiPlayerGameMode connection
public net.minecraft.world.level.chunk.PalettedContainer data
# NOTE: the Fabric access widener also widened PalettedContainer$Data's "palette" and "storage".
# Data is a record -- `record Data<T>(Configuration configuration, BitStorage storage, Palette<T> palette)` --
# so those are record components, not declared fields, and NeoForge's source-level AT validator rejects
# them ("target does not exist"). Widening the record type itself (above) is enough: its accessors
# storage()/palette() are then reachable, which is what WorldConversionFactory now calls.
public net.minecraft.client.renderer.texture.SpriteContents mipmapStrategy
public net.minecraft.world.level.block.StairBlock baseState
